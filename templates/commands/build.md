---
description: Implement the plan
argument-hint: <path-to-plan>
model: opus
---

Read the plan at `$ARGUMENTS` and implement it. If no path is provided, stop and ask the user to provide one via `AskUserQuestion`.
